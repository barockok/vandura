roles:
  pm:
    agents: [atlas, scribe]
    tool_tiers:
      mcp__postgres__query: { max_tier: 3 }
      mcp__postgres__execute: { max_tier: 3 }
      upload_file: { max_tier: 1 }
      mcp-confluence: { max_tier: 2 }
      mcp-gdocs: { max_tier: 2 }
      mcp-gcs: { max_tier: 1 }

  engineering:
    agents: [atlas, scribe, courier, sentinel]
    tool_tiers:
      mcp__postgres__query: { max_tier: 3 }
      mcp__postgres__execute: { max_tier: 3 }
      upload_file: { max_tier: 1 }
      mcp-confluence: { max_tier: 2 }
      mcp-gdocs: { max_tier: 2 }
      mcp-rest: { max_tier: 3 }
      mcp-gcs: { max_tier: 2 }

  business:
    agents: [atlas, scribe]
    tool_tiers:
      mcp__postgres__query: { max_tier: 3 }
      mcp__postgres__execute: { max_tier: 3 }
      upload_file: { max_tier: 1 }
      mcp-confluence: { max_tier: 1 }
      mcp-gdocs: { max_tier: 2 }
      mcp-gcs: { max_tier: 1 }
